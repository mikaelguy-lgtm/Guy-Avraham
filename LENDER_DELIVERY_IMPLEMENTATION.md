# מימוש מסירת תיקים לחברות מימון

## מיפוי המערכת הקיימת

| יכולת | רכיב קיים | אופן השימוש במודול החדש |
| --- | --- | --- |
| חברות מימון | `lenders` | הטבלה מורחבת לפרופיל חברה מלא ונשארת מקור האמת לחברות קיימות. |
| משתמשי חתם | `lender_users`, Firebase roles | נשמרים לתאימות היסטורית בלבד. הזרימה החדשה משתמשת באנשי קשר ציבוריים ו־OTP ללא חשבון קבוע. |
| שליחות | `lender_submissions` | נשמרות ללא מחיקה לתאימות. הזרימה החדשה משתמשת בגרסת תיק ובשליחה ברמת חברה. |
| קישורי הזמנה | `lender_invite_tokens` | נשמרים לתאימות. קישורים חדשים נוצרים לכל איש קשר ושומרים hash בלבד. |
| חשיפת זהות | `identity_reveal_requests` | נשמרת להיסטוריה ואינה משמשת בזרימה החדשה; גישה מלאה נפתחת רק לאחר החלטה ו־OTP. |
| הצעות | `loan_offers` | נשמרות להיסטוריה אך אין טופס הצעה בפורטל החדש. |
| מסמכים | `documents`, `StorageService` | קבצים נשארים ב־MinIO פרטי ומועתקים לנתיב immutable לכל גרסת תיק. |
| הסוואה | `snapshot.ts` | מורחבת לשירות הסוואה דטרמיניסטי של כל נתוני התיק והטקסטים החופשיים. |
| PDF | `pdf.ts` | אותו שירות מורחב ל־PDF מוסווה ול־PDF מלא ממותגים. |
| מייל | `EmailService`, `email_logs` | שירות SMTP הקיים משמש Worker מבוסס outbox ללא attachments. |
| התראות | `notifications` | סוגי אירועי מסירה חדשים נכתבים לאותה טבלה. |
| Audit | `audit_logs` | פעולות Firebase של יועץ ואדמין ממשיכות להירשם; אירועי חברה נשמרים ב־submission events בטוחים. |
| Rate limiting | Redis | משמש גם endpoints ציבוריים, OTP וקישורים. |
| Realtime | לא קיים | נוסף SSE מאומת ליועץ ולאדמין; הנתונים נטענים מחדש לאחר אירוע. |

## הרחבות שמומשו

- אנשי קשר רבים לכל חברה, soft delete ותחומי פעילות.
- לוח ימי עסקים ישראלי מרכזי עם חריגים במסד.
- גרסת תיק immutable עם snapshot מלא מוצפן, snapshot מוסווה ועותקי מסמכים פרטיים.
- שליחה ברמת חברה והזמנה נפרדת לכל איש קשר.
- החלטה ראשונה שהושלמה קובעת, OTP חד־פעמי וגישה מלאה לשבעה ימים.
- sessions חיצוניים ב־HttpOnly cookie, PDF מלא, מסמכים ו־ZIP בהרשאה בזמן אמת.
- outbox ו־worker אידמפוטנטיים עם advisory lock, reminders ופקיעות.
- מסכי ניהול, תצוגה מקדימה ליועץ, תגובות חברות ופורטל ציבורי בעברית וב־RTL.

## רכיבי המימוש

- `PostgresLenderDeliveryService` מרכז עסקאות, יצירת גרסאות, החלטות, OTP, sessions, Outbox, תזכורות ופקיעות.
- `CaseRedactionService` יוצר snapshot מוסווה דטרמיניסטי ודוח הסוואה ללא הערכים המקוריים.
- `IsraelBusinessCalendarService` מחשב שני ימי עסקים ב־`Asia/Jerusalem`, חגים וחריגי מנהל.
- `DeliveryTokenService` מפיק Tokens ו־OTP קריפטוגרפיים; במסד נשמר hash בלבד.
- `DeliveryEventBroker` מספק SSE מסונן לפי בעלות תיק או הרשאת מנהל.
- `ExternalDeliveryPortal` מספק בדיקה מוסווית ופורטל מלא ללא חשבון קבוע.
- `AdminFinancingCompaniesView`, `AdminBusinessCalendarView` ו־`AdminCompanySubmissionsView` מספקים תפעול ובקרה מלאים.

## תאימות והגירה

ההגירה אינה מוחקת טבלאות, הצעות, בקשות חשיפה, מסמכים או נתונים היסטוריים. ה־routes הישנים נשארים זמינים לתיקים היסטוריים ומסומנים כ־deprecated בתיעוד; כל שליחה חדשה מתבצעת דרך `/api/clients/:clientId/delivery/*`.
